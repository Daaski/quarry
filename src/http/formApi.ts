import * as T from './types'
import {$clientAuth} from "http/index";
import {AxiosResponse} from "axios";


/*
export const getObjects: T.GetObjects = async () => {
    const res: AxiosResponse<T.Object[]> = await $clientAuth.get()

    return res.data
}

export const getCustomers: T.GetCustomers = async () => {
    const res: AxiosResponse<T.Customer[]> = await $clientAuth.get()

    return res.data
}

export const getCarNumbers: T.GetCarNumbers = async () => {
    const res: AxiosResponse<T.CarNumber[]> = await $clientAuth.get()

    return res.data
}

export const getDrivers: T.GetDrivers = async () => {
    const res: AxiosResponse<T.Driver[]> = await $clientAuth.get()

    return res.data
}

export const getGoods: T.GetGoods = async () => {
    const res: AxiosResponse<T.Goods[]> = await $clientAuth.get()

    return res.data
}

export const sendOrder: T.SendOrder = async ({object,
                                                 count,
                                                 description,
                                                 carNumber,
                                                 customer,
                                                 good,
                                                 driver}) => {
    await $clientAuth.post()

}*/
